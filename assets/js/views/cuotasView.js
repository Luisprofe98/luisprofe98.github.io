import { borrarFee, guardarFee, listarFee } from "../services/feeService.js";
import { formatoEuros } from "../utils/moneyUtils.js";
import { abrirModal, cerrarModal, estadoBadge, formData, textoSeguro } from "../utils/uiUtils.js";
import { mostrarAviso } from "../utils/alertUtils.js";

export async function renderCuotas(pageRoot) { const feeList = await listarFee(); pageRoot.innerHTML = `<section class="container-fluid px-0"><p class="module-label text-uppercase small mb-2">Cuotas y pagos</p><h1 class="h3 mb-3">Cuotas</h1><div id="alertRoot"></div><div class="alert alert-warning">Pago online pendiente de configurar</div><div class="d-flex justify-content-end mb-3"><button class="btn btn-clubox" id="newFee" type="button">+ Nueva cuota</button></div><div class="card card-clubox"><div class="card-body table-responsive"><table class="table table-hover align-middle mb-0"><thead><tr><th>Nombre</th><th>Importe</th><th>Periodicidad</th><th>Destinatario</th><th>Estado</th><th>Acciones</th></tr></thead><tbody>${feeList.map((itemData) => `<tr><td>${textoSeguro(itemData.nombre)}</td><td>${formatoEuros(itemData.importe)}</td><td>${textoSeguro(itemData.periodicidad)}</td><td>${textoSeguro(itemData.destinatario)}</td><td>${estadoBadge(itemData.estado)}</td><td><button class="btn btn-sm btn-outline-clubox" data-edit="${itemData.id}" type="button">Editar</button> <button class="btn btn-sm btn-outline-danger" data-delete="${itemData.id}" type="button">Eliminar</button></td></tr>`).join("") || '<tr><td colspan="6">Sin cuotas.</td></tr>'}</tbody></table></div></div></section>${feeModal()}`; document.getElementById("newFee").addEventListener("click", () => editFee()); pageRoot.querySelectorAll("[data-edit]").forEach((buttonData) => buttonData.addEventListener("click", () => editFee(feeList.find((itemData) => itemData.id === buttonData.dataset.edit)))); pageRoot.querySelectorAll("[data-delete]").forEach((buttonData) => buttonData.addEventListener("click", async () => { await borrarFee(buttonData.dataset.delete); mostrarAviso(pageRoot.querySelector("#alertRoot"), "succed", "Cuota eliminada"); renderCuotas(pageRoot); })); document.getElementById("feeForm").addEventListener("submit", async (event) => { event.preventDefault(); const resultData = await guardarFee(formData(event.currentTarget)); if (!resultData.valido) return mostrarAviso(pageRoot.querySelector("#alertRoot"), "failed", "Importe no valido"); cerrarModal("feeModal"); renderCuotas(pageRoot); }); }
function feeModal() { return `<div class="modal fade" id="feeModal" tabindex="-1"><div class="modal-dialog"><form class="modal-content" id="feeForm"><div class="modal-header"><h2 class="h5 modal-title">Cuota</h2><button class="btn-close" type="button" data-bs-dismiss="modal" aria-label="Cerrar"></button></div><div class="modal-body"><input type="hidden" id="feeId" name="id"><div class="vstack gap-2"><label class="form-label">Nombre<input class="form-control" id="feeName" name="nombre" required></label><label class="form-label">Importe<input class="form-control" id="feeAmount" name="importe" type="number" min="0.01" step="0.01" required></label><label class="form-label">Periodicidad<select class="form-select" id="feePeriod" name="periodicidad"><option>unica</option><option>mensual</option><option>trimestral</option><option>anual</option></select></label><label class="form-label">Tipo destinatario<select class="form-select" id="feeTarget" name="destinatario"><option>jugador</option><option>socio</option><option>formulario</option></select></label><label class="form-label">Estado<select class="form-select" id="feeState" name="estado"><option>activo</option><option>inactivo</option></select></label></div></div><div class="modal-footer"><button class="btn btn-outline-secondary" type="button" data-bs-dismiss="modal">Cancelar</button><button class="btn btn-clubox" type="submit">Guardar cuota</button></div></form></div></div>`; }
function editFee(itemData = {}) { document.getElementById("feeId").value = itemData.id || ""; document.getElementById("feeName").value = itemData.nombre || ""; document.getElementById("feeAmount").value = itemData.importe || ""; document.getElementById("feePeriod").value = itemData.periodicidad || "mensual"; document.getElementById("feeTarget").value = itemData.destinatario || "jugador"; document.getElementById("feeState").value = itemData.estado || "activo"; abrirModal("feeModal"); }
